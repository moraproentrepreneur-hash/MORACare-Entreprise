'use client';

import React from 'react';
import { SettingsModule } from '@/components/modules/SettingsModule';

/** Paramètres globaux de la plateforme (UG01 §10). */
export default function GlobalSettingsPage() {
  return <SettingsModule />;
}
